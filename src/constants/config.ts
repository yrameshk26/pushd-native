export const API_BASE_URL = __DEV__
  ? 'http://localhost:3000'
  : 'https://pushd.fit';

export const TOKEN_STORAGE_KEY = 'pushd_access_token';
export const REFRESH_TOKEN_STORAGE_KEY = 'pushd_refresh_token';
